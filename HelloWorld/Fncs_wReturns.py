from math import sqrt
def square(number):
    print(f"Calculating the square of {number}")
    return number * number

def squareroot(number):
    print(f"Calculating the square root of {number}")
    return sqrt(number)
